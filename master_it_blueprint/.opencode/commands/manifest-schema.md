---
description: Run manifest schema inspection
agent: manifest-schema
---

Inspect the manifest schema and write the canonical schema contract.

Read the shared pipeline context first.

If $ARGUMENTS is supplied, treat it as additional repository context.

Write:

.manifest-pipeline/01-schema-contract.json

Do not generate the final manifest.
