---
description: Extract the normalized manifest domain model
agent: manifest-domain
---

Extract the normalized domain model.

Read the existing:

.manifest-pipeline/01-schema-contract.json

and the original repository requirements.

Write:

.manifest-pipeline/02-domain-model.json

Do not generate the final manifest.

Additional context:

$ARGUMENTS
