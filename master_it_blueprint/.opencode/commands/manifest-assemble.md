---
description: Assemble the production manifest
agent: manifest-assemble
---

Generate the production manifest using all prior pipeline artifacts and the
original requirements.

Write ONLY the JSON manifest to:

.manifest-pipeline/06-manifest.json

Do not return a Markdown manifest.

Additional context:

$ARGUMENTS
